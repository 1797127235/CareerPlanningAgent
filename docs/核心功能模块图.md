# 职途智析 — 核心功能模块图

## 系统整体架构模块图

```mermaid
flowchart TB
    %% 数据基座层
    subgraph DataLayer["📊 数据基座层"]
        D1["招聘语料<br/>150万+条"]
        D2["AEI 数据<br/>409MB"]
        D3["O*NET<br/>任务框架"]
        D4["岗位知识图谱<br/>45节点·101边"]
    end

    %% 七大功能模块
    subgraph Modules["🧩 七大核心功能模块"]
        direction TB

        subgraph M1["① 能力画像"]
            M1a["简历上传<br/>PDF/图片/文字"]
            M1b["LLM 结构化提取"]
            M1c["SJT 情景测评<br/>五维软技能"]
            M1d["图谱坐标定位<br/>完整度/竞争力评分"]
        end

        subgraph M2["② 岗位图谱"]
            M2a["45节点可视化"]
            M2b["晋升路径 L1-L5"]
            M2c["换岗路径 101条"]
            M2d["AEI AI影响分析<br/>替代压力/协同杠杆"]
        end

        subgraph M3["③ JD 诊断"]
            M3a["粘贴真实JD"]
            M3b["四维匹配评分"]
            M3c["技能差距清单"]
            M3d["图谱关联分析"]
        end

        subgraph M4["④ 发展报告"]
            M4a["AI综合评价"]
            M4b["方向对齐分析"]
            M4c["三段式补法地图<br/>learn/practice/both"]
            M4d["分阶段行动计划<br/>导出PDF"]
        end

        subgraph M5["⑤ 成长档案"]
            M5a["项目追踪"]
            M5b["求职追踪"]
            M5c["档案精修"]
            M5d["Delta对比"]
        end

        subgraph M6["⑥ AI 模拟面试"]
            M6a["6方向Skill驱动"]
            M6b["画像联动出题"]
            M6c["历史弱项复习"]
            M6d["空答案强制0分<br/>反幻觉过滤"]
        end

        subgraph M7["⑦ AI 教练"]
            M7a["13个Coach Skill"]
            M7b["Progressive Disclosure"]
            M7c["自然语言交互"]
        end
    end

    %% 技术支撑层
    subgraph TechLayer["⚙️ 技术支撑层"]
        T1["LangGraph<br/>Supervisor + 6 Agent"]
        T2["通义千问<br/>qwen-plus/qwen-max"]
        T3["text-embedding-v3<br/>向量检索"]
        T4["三层幻觉防护<br/>架构/Prompt/过滤"]
    end

    %% 核心闭环连接
    M1 -->|"画像数据"| M3
    M2 -->|"岗位需求"| M3
    M3 -->|"诊断结果"| M4
    M4 -->|"行动计划"| M5
    M5 -->|"成长数据"| M6
    M6 -->|"skill_gaps回灌"| M5
    M5 -->|"Delta更新"| M4

    %% 数据支撑
    D1 & D2 & D3 --> D4
    D4 --> M2
    D4 --> M3

    %% 技术支撑
    TechLayer --> Modules

    %% AI教练贯穿
    M7 -.->|"贯穿全系统"| M1 & M2 & M3 & M4 & M5 & M6
```

---

## 核心业务闭环

```mermaid
flowchart LR
    A["画像构建"] --> B["人岗匹配"] --> C["发展报告"] --> D["成长档案"] --> E["模拟面试"]
    E -->|"skill_gaps<br/>回灌"| D
    D -->|"Delta<br/>更新"| C
    C -->|"行动执行"| D

    style A fill:#a29bfe,color:#fff
    style B fill:#74b9ff,color:#fff
    style C fill:#55efc4,color:#333
    style D fill:#ffeaa7,color:#333
    style E fill:#fab1a0,color:#333
```

---

## 模块详细拆解

### ① 能力画像模块

```mermaid
flowchart LR
    subgraph Input["输入"]
        I1["PDF简历"]
        I2["图片简历"]
        I3["文字粘贴"]
    end

    subgraph Process["处理流程"]
        P1["ResumeSDK 首轮"]
        P2["LLM 结构化抽取"]
        P3["qwen-vl-ocr 并行OCR"]
        P4["SJT 情景测评"]
    end

    subgraph Output["输出"]
        O1["技能列表 + 熟练度"]
        O2["项目/实习经历"]
        O3["教育背景"]
        O4["五维软技能雷达"]
        O5["图谱坐标定位"]
    end

    I1 --> P1 --> P2
    I2 --> P3 --> P2
    I3 --> P2
    P4 --> O4
    P2 --> O1 & O2 & O3
    O1 & O4 --> O5
```

### ② 岗位知识图谱模块

```mermaid
flowchart TB
    subgraph Graph["图谱结构"]
        N["45个岗位节点<br/>20个岗位族"]
        E1["晋升边<br/>同岗族 L1→L5"]
        E2["换岗边<br/>跨岗族迁移 101条"]
        E3["邻接边<br/>技能高重合"]
    end

    subgraph NodeDetail["节点属性"]
        ND1["必备技能 / 加分技能"]
        ND2["薪资分布"]
        ND3["AEI 替代压力 0-100"]
        ND4["AEI 协同杠杆 0-100"]
        ND5["典型项目模板"]
    end

    subgraph Action["用户操作"]
        A1["可视化探索"]
        A2["点击查看详情"]
        A3["一键跳转模拟面试"]
    end

    Graph --> NodeDetail
    NodeDetail --> Action
```

### ③ JD 诊断模块

```mermaid
flowchart LR
    JD["粘贴真实JD"] --> Parse["LLM提取<br/>核心技能需求"]

    Parse --> Match["四维匹配算法"]

    subgraph Dimensions["四维评分"]
        D1["职业技能 50%"]
        D2["发展潜力 25%"]
        D3["基础要求 15%"]
        D4["职业素养 10%"]
    end

    Match --> Dimensions --> Score["综合匹配分"]
    Score --> Gap["技能差距清单<br/>高优先级标红"]
    Score --> AI["AI影响分析<br/>替代压力/协同杠杆/zone"]
    Score --> Trans["推荐转岗路线<br/>需补技能+预估时间"]
```

### ④ 发展报告模块

```mermaid
flowchart TB
    Input["画像 + JD诊断 + 图谱数据"] --> Report

    subgraph Report["发展报告内容"]
        R1["AI综合评价<br/>evidence引用"]
        R2["方向对齐分析<br/>最优+备选"]
        R3["三段式补法地图"]
        R4["分阶段行动计划<br/>0-2周/2-6周/6-12周"]
    end

    subgraph Remedy["补法类型"]
        L["learn 学概念"]
        P["practice 做项目"]
        B["both 先学后做"]
    end

    R3 --> Remedy
    Report --> Export["手动编辑 + AI润色 + PDF导出"]
```

### ⑤ 成长档案模块

```mermaid
flowchart LR
    subgraph Track["追踪维度"]
        T1["项目追踪<br/>技术栈/状态/反思"]
        T2["求职追踪<br/>投递进度/面试复盘"]
        T3["档案精修<br/>AI诊断空洞+学生填空"]
    end

    subgraph Delta["Delta机制"]
        D1["时间线记录"]
        D2["前后对比"]
        D3["进度可视化"]
    end

    Track --> Delta
    Delta -->|"回灌"| Report["更新发展报告"]
    Delta -->|"数据注入"| Interview["模拟面试出题"]
```

### ⑥ AI 模拟面试模块

```mermaid
flowchart TB
    subgraph Input["出题数据源"]
        L1["简历画像"]
        L2["成长档案<br/>gap_skills"]
        L3["历史面试<br/>skill_gaps"]
        L4["JD要求"]
    end

    subgraph Skill["6方向Skill"]
        S1["C++系统开发"]
        S2["前端开发"]
        S3["Java后端"]
        S4["算法工程师"]
        S5["产品经理"]
        S6["测试开发"]
    end

    subgraph Guard["保障机制"]
        G1["空答案强制0分"]
        G2["30+占位符反幻觉"]
        G3["题库预缓存"]
    end

    Input --> LLM["LLM 个性化生成"] --> Skill
    Skill --> Questions["面试题目<br/>3/5/10题可选"]
    Questions --> Eval["逐题评分<br/>准确/完整/深度/表达"]
    Eval --> Feedback["strengths + improvements<br/>+ suggested_answer"]
    Feedback --> Gaps["skill_gaps → 回灌档案"]

    Guard -.-> Eval
    Guard -.-> Questions
```

### ⑦ AI 教练模块

```mermaid
flowchart LR
    User["用户自然语言"] --> Router["三级意图识别<br/>正则→语义→LLM"]

    Router --> Skills["13个Coach Skill<br/>按需加载"]

    subgraph SkillList["Skill 覆盖场景"]
        SK1["求职策略"]
        SK2["简历优化"]
        SK3["面试复盘"]
        SK4["方向选择"]
        SK5["...等13项"]
    end

    Skills --> SkillList
    SkillList --> Response["结合画像/档案<br/>实时个性化建议"]
```

---

## AI Agent 协作体系

```mermaid
flowchart TD
    User["用户输入"] --> Supervisor["Supervisor 路由器"]

    Supervisor -->|"正则/语义命中"| Direct["直接路由"]
    Supervisor -->|"复杂长句"| LLM_Route["LLM 分类兜底"]

    Direct --> A1["navigator<br/>图谱导航"]
    Direct --> A2["profile<br/>画像管理"]
    Direct --> A3["jd<br/>JD诊断"]
    LLM_Route --> A4["growth<br/>成长档案"]
    LLM_Route --> A5["coach<br/>AI教练·13Skill"]
    LLM_Route --> A6["search<br/>搜索"]

    subgraph LLMLayer["大模型层"]
        Q1["qwen-plus<br/>轻任务"]
        Q2["qwen-max<br/>强推理"]
        V["text-embedding-v3"]
    end

    A1 & A2 & A3 & A4 & A5 & A6 --> LLMLayer
```

---

## 四层系统架构

```mermaid
flowchart TB
    subgraph Frontend["前端展示层 · React 19 + Vite"]
        F1["画像页"] 
        F2["图谱页"]
        F3["JD诊断页"]
        F4["报告页"]
        F5["档案页"]
        F6["面试页"]
        F7["教练面板"]
    end

    subgraph Backend["后端服务层 · FastAPI"]
        B1["18个Router"]
        B2["Services层"]
        B3["SQLite + ORM"]
    end

    subgraph Agent["AI Agent层 · LangGraph"]
        AG1["Supervisor"]
        AG2["6专家Agent"]
        AG3["13 Coach Skill"]
    end

    subgraph Model["大模型与向量层"]
        M1["qwen-plus"]
        M2["qwen-max"]
        M3["text-embedding-v3"]
    end

    Frontend -->|"REST / SSE"| Backend
    Backend -->|"调用"| Agent
    Agent -->|"调用"| Model
    Backend -->|"直接调用"| Model
```
